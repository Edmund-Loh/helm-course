list:
  values:
    example: {{ list 1 2 3 | toYaml | nindent 6}}
# if you use `indent 6`, it will indent without inserting a newline
my-dict: {{ dict "primitive" "my string" "complex" (dict "key1" "value1") | toYaml | nindent 2 }}